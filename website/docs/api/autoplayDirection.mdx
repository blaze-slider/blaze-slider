---
sidebar_position: 8
---

# `autoplayDirection`

import { ExampleSlider } from '@site/src/components/ExampleSlider'

It indicates which direction the autoplay should run in, `to right` or `to left`

This is usually the desired behavior which is why it's enabled by default

## possible values

```typescript
autoplayDirection: 'to left' | 'to right'
```

## default

```typescript
autoplayDirection: 'to left'
```

<br />

### Example `autoplayDirection: "to left"` (default)

```javascript
new BlazeSlider(el, {
  all: {
    enableAutoplay: true,
    autoplayDirection: 'to left',
    slidesToShow: 3,
  },
})
```

<ExampleSlider
  config={{
    all: {
      enableAutoplay: true,
      autoplayDirection: 'to left',
      slidesToShow: 3,
    },
  }}
>
  <div> 1 </div>
  <div> 2 </div>
  <div> 3 </div>
  <div> 4 </div>
  <div> 5 </div>
  <div> 6 </div>
</ExampleSlider>

<br />

### Example `autoplayDirection: "to right"`

```javascript
new BlazeSlider(el, {
  all: {
    enableAutoplay: true,
    autoplayDirection: 'to right',
    slidesToShow: 3,
  },
})
```

<ExampleSlider
  config={{
    all: {
      enableAutoplay: true,
      autoplayDirection: 'to right',
      slidesToShow: 3,
    }
}}>

  <div> 1 </div>
  <div> 2 </div>
  <div> 3 </div>
  <div> 4 </div>
  <div> 5 </div>
  <div> 6 </div>
</ExampleSlider>
